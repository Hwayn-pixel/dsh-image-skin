# dsh-image-skin — bundle patch layer (declared by dsh.bundle.patch).
# Registers this plugin's row into the web profile's plugin roster.
- insert:
    - id: ui-image-skin
      name: 'dsh-image-skin'
